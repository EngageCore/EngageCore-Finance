import zhCN from './langs/zh-cn';
import enUS from './langs/en-us';

const locales = {
  'zh-CN': zhCN,
  'en-US': enUS,
  /** Nested messages for id-ID: flat strings live in localTranslations.id_ID */
  'id-ID': enUS
};

export default locales;
